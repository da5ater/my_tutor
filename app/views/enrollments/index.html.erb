<div class="container py-5">
  <% if notice.present? %>
    <div class="alert alert-success alert-dismissible fade show border-0 shadow-sm mb-4 d-flex align-items-center" role="alert">
      <i class="fa-solid fa-circle-check me-2"></i>
      <div><%= notice %></div>
      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
  <% end %>

  <% content_for :title, "All Enrollments" %>

  <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4 animate-fade-in">
    <div>
      <h3 class="fw-bold text-dark mb-1 font-heading">
        Enrollments
        <span class="badge bg-info text-white"><%= @enrollments.count %></span>
      </h3>
      <p class="text-secondary mb-0">Browse and manage all student enrollments and ratings.</p>
    </div>
    <div>
      <%= link_to new_enrollment_path, class: "btn btn-primary px-4 py-2.5 rounded-3 fw-bold shadow-sm d-flex align-items-center gap-2" do %>
        <i class="fa-solid fa-plus"></i> New Enrollment
      <% end %>
    </div>
  </div>

  <div class="card border-0 shadow-sm rounded-4 overflow-hidden bg-white">
    <div class="table-responsive">
      <table class="table table-striped table-bordered table-hover align-middle mb-0">
        <thead class="table-light border-bottom">
          <tr>
            <th class="px-4 py-3 text-secondary font-heading fw-bold" style="width: 10%;">ID</th>
            <th class="px-4 py-3 text-secondary font-heading fw-bold" style="width: 25%;">User / Student</th>
            <th class="px-4 py-3 text-secondary font-heading fw-bold" style="width: 25%;">Course</th>
            <th class="px-4 py-3 text-secondary font-heading fw-bold" style="width: 12%;">Price Paid</th>
            <th class="px-4 py-3 text-secondary font-heading fw-bold" style="width: 10%;">Rating</th>
            <th class="px-4 py-3 text-secondary font-heading fw-bold" style="width: 18%;">Review</th>
            <th class="px-4 py-3 text-secondary font-heading fw-bold text-end" style="width: 10%;">Actions</th>
          </tr>
        </thead>
        <tbody>
          <% if @enrollments.any? %>
            <% @enrollments.each do |enrollment| %>
              <tr id="<%= dom_id enrollment %>">
                <td class="px-4 py-3 fw-semibold text-muted">#<%= enrollment.id %></td>
                <td class="px-4 py-3">
                  <div class="d-flex align-items-center gap-2">
                    <div class="rounded-circle bg-light d-flex align-items-center justify-content-center border" style="width: 32px; height: 32px; flex-shrink: 0;">
                      <i class="fa-solid fa-circle-user text-secondary"></i>
                    </div>
                    <div class="text-truncate" style="max-width: 180px;">
                      <% if enrollment.user %>
                        <%= link_to enrollment.user.email, user_path(enrollment.user), class: "text-decoration-none text-dark hover-underline fw-semibold" %>
                      <% else %>
                        <span class="text-muted italic">Unknown user</span>
                      <% end %>
                    </div>
                  </div>
                </td>
                <td class="px-4 py-3">
                  <% if enrollment.course %>
                    <%= link_to enrollment.course.title, course_path(enrollment.course), class: "text-decoration-none text-dark hover-underline fw-semibold" %>
                  <% else %>
                    <span class="text-muted italic">Deleted course</span>
                  <% end %>
                </td>
                <td class="px-4 py-3 fw-bold text-dark">
                  <%= number_to_currency(enrollment.price, precision: 0) %>
                </td>
                <td class="px-4 py-3">
                  <% if enrollment.rating.present? %>
                    <span class="text-warning"><i class="fa-solid fa-star"></i> <%= enrollment.rating %>/5</span>
                  <% else %>
                    <span class="text-muted small">No rating</span>
                  <% end %>
                </td>
                <td class="px-4 py-3 text-secondary text-truncate small" style="max-width: 180px;" title="<%= enrollment.review %>">
                  <%= enrollment.review.presence || "No review yet" %>
                </td>
                <td class="px-4 py-3 text-end">
                  <div class="d-inline-flex gap-2">
                    <%= link_to enrollment, class: "btn btn-sm btn-outline-secondary d-flex align-items-center gap-1 px-3 py-1.5 rounded-2" do %>
                      <i class="fa-solid fa-eye small"></i> Show
                    <% end %>
                    <%= link_to edit_enrollment_path(enrollment), class: "btn btn-sm btn-outline-primary d-flex align-items-center gap-1 px-3 py-1.5 rounded-2" do %>
                      <i class="fa-solid fa-pen-to-square small"></i> Edit
                    <% end %>
                    <%= button_to enrollment, method: :delete, data: { turbo_confirm: "Are you sure you want to delete this enrollment?" }, class: "btn btn-sm btn-outline-danger d-flex align-items-center gap-1 px-3 py-1.5 rounded-2" do %>
                      <i class="fa-solid fa-trash small"></i> Delete
                    <% end %>
                  </div>
                </td>
              </tr>
            <% end %>
          <% else %>
            <tr>
              <td colspan="7" class="text-center py-5 text-muted">
                <i class="fa-solid fa-lock-open fa-2x mb-3 d-block opacity-50"></i>
                No enrollments registered yet.
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>

  <div class="mt-4">
    <%= link_to root_path, class: "btn btn-link px-0 text-decoration-none d-inline-flex align-items-center gap-2 fw-semibold text-secondary" do %>
      <i class="fa-solid fa-arrow-left"></i> Back to Home
    <% end %>
  </div>
</div>
